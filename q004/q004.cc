/**
 * Q004 ソートアルゴリズム
 *
 * ListManagerクラスを生成して、小さい順に並び変えた上でCheckResult()を呼び出してください。
 *
 * 実装イメージ:
 * ListManager data;
 * // TODO 並び換え
 * data.CheckResult();
 *
 * - ListManagerクラスを修正してはいけません
 * - ListManagerクラスの dataList を直接変更してはいけません
 * - ListManagerクラスの比較 Compare と入れ替え Exchange を使って実現してください
 */
#include <iostream>
#include "list_manager.h"

using namespace std;

